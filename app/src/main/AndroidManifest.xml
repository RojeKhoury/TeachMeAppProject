<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teachmeapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <permission
        android:name="com.example.teachmeapp.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.teachmeapp.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TeacherProfilePageOfferedLessons"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".StudentPendingRequestDetailsPage" />
        <activity android:name=".TeacherPendingRequestDetailsPage" />
        <activity android:name=".StudentPendingRequest" />
        <activity android:name=".TeacherPendingRequests" />
        <activity android:name=".EditTeacherInfo" />
        <activity android:name=".History" />
        <activity
            android:name=".TeacherLessonsAddOrRemove"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".maps_activity_get_location"
            android:label="@string/title_activity_maps_get_location">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScheduleExpandingButton"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".RojeTest"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".RecommendationForStudent"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".Schedule"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".Chat.Chats"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".Chat.ChatWindow"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".HistoryClassesPage"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".HomePageStudent"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".HomePageTeacher"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".LoginAsTeacherOrStudent"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".ConversationChatPage"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".SearchForTeacher"
            android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar" />
        <activity
            android:name=".ProfilePageOfTeacherForStudent"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".HamburgerMenu"
            android:theme="@style/AppThemeNoBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".SignUp"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".RequestLessons"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".Login"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".test_profile_page"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeNoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>